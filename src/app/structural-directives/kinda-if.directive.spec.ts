import { KindaIfDirective } from './kinda-if.directive';

describe('KindaIfDirective', () => {
  it('should create an instance', () => {
    const directive = new KindaIfDirective();
    expect(directive).toBeTruthy();
  });
});
