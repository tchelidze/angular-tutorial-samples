import { SdDemoDirective } from './sd-demo.directive';

describe('SdDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new SdDemoDirective();
    expect(directive).toBeTruthy();
  });
});
